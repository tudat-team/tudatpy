# Example validation corrections

This index identifies every correction from the PR #1000 / examples PR #157 validation, its verification, and where to find its commit. Full execution and plot-review status is in [EXAMPLE_VALIDATION_PROGRESS.md](EXAMPLE_VALIDATION_PROGRESS.md). The user clarified on 2026-09-12 that commits and pushes must wait until all corrections to a branch are complete. Keep ongoing corrections and log updates local, then publish the completed branch with CI cost considered. Earlier incremental publication was a misunderstanding. Unfinished validation is explicitly identified below.

## Completed example corrections

Commit: [28e5337 — Correct validated example execution and scientific plots](https://github.com/tudat-team/tudatpy-examples/commit/28e5337114f02c01b7cbf695c9cc883c4e479f0c), branch `feature/data-refactor` (PR #157).

| File | Correction | Verification |
| --- | --- | --- |
| [estimation/mro_range_estimation.py](examples/tudatpy/estimation/mro_range_estimation.py) | Remove machine-specific kernel import path. | Complete three-mission run, eight plots reviewed; no deprecations. |
| [mission_design/hodographic_shaping_mga_optimization.py](examples/tudatpy/mission_design/hodographic_shaping_mga_optimization.py) | Guard multiprocessing entry points so imported worker modules cannot recursively start islands. | Full 40 generations, population 1,000; both plots reviewed; no deprecations. |
| [mission_design/low_thrust_earth_mars_transfer_window.py](examples/tudatpy/mission_design/low_thrust_earth_mars_transfer_window.py) | Correct meter coordinate labels and remove a second mass division from thrust acceleration. | Complete 200 × 442 grid and both trajectories; all five plots reviewed; no deprecations. |
| [pygmo/asteroid_orbit_optimization/aoo_design_space_exploration.py](examples/tudatpy/pygmo/asteroid_orbit_optimization/aoo_design_space_exploration.py) | Use each varied parameter's constraint colors, correct distance/angular units, and select inclination for the inclination/node response surface. | Full 2,000 Monte Carlo and 2,401 factorial simulations; corrected plotting blocks replayed on saved arrays with coordinate/color assertions; all three plots reviewed. |

Local evidence: `.validation/examples-pr157/pr1000-modern/<path-with-slashes-replaced-by-__>/`, including `result.json`, figures and numerical arrays. The asteroid design-space directory also contains `plot-revalidation.json`.

## Completed library plotting corrections

Files: [porkchop plotting](src/tudatpy/trajectory_design/porkchop/_plot_porkchop.py), [plotting helpers](src/tudatpy/plotting/_helpers.py).

Porkchop colorbars now identify km/s and km²/s², matching the scaled values. A raw plotting-helper docstring prevents an invalid-escape deprecation warning. Full Earth–Mars and low-thrust examples and their plots were reviewed. The combined plotting/MPC focused suite passed 31 tests with deprecations treated as errors (`.validation/examples-pr157/plotting-and-mpc-tests.log`). Commit: [fcfaa048a — Correct porkchop units and plotting docstring escapes](https://github.com/tudat-team/tudatpy/commit/fcfaa048ac6157df35bf704809408df68efb0b2a), branch `codex/pr900-review-by-category-20260912` (PR #1000).

## Completed MPC library corrections

- [Legacy reader](src/tudatpy/data/mpc/_legacy.py), [regression test](tests/test_tudatpy/test_data_legacy_calls.py): restore the pre-refactor Note 2 filter inside the removable compatibility module, retain space records for inspection, and resolve identifiers even when filtering is disabled. Commit [44bab9d3d](https://github.com/tudat-team/tudatpy/commit/44bab9d3d1dcde47e063328e1604e442833c2a97). All 51 legacy-call and compatibility-boundary tests passed. The original basic-MPC ingestion prefix runs unchanged, including its WISE preview. Evidence: `.validation/examples-pr157/legacy-reader-regressions.log` and `compare-mpc-readers-fixed.log`. Full original-example compatibility remains pending.
- [Modern MPC reader](src/tudatpy/data_input/tracking_data/mpc/mpc.py), [regression test](tests/test_tudatpy/test_mpc_query.py): restore observatory names, counts, catalog/space filtering, empty-batch behavior and unknown-code handling. Commit [5b43b9540](https://github.com/tudat-team/tudatpy/commit/5b43b954048a81e725ac2aa0914c92f09feaff59). Included in the 31 passing focused MPC/plotting tests; related example runs and figures reviewed.

## Completed MPC estimation correction

[estimation/estimation_with_mpc.py](examples/tudatpy/estimation/estimation_with_mpc.py): explicitly enable weights and catalogue debiasing on ingestion **and** `apply_corrections=True` during collection construction. Correct stale space-observation and body-creation prose. Commit [93770dc](https://github.com/tudat-team/tudatpy-examples/commit/93770dca6a1ed2f59b519a2165519ffbef92bb5e), pushed to examples PR #157.

The complete rerun passed in 44.4 seconds on the spare single CPU; all six figures were visually reviewed and agree with the stored reference trends. Final position difference from SPICE: 35.23 km. Original/new ingestion agrees within 7.1e-14 radians and 2.1 microseconds, with identical weights and link ordering. No modern deprecation warnings; expected legacy warnings were observed. Evidence: `.validation/examples-pr157/mpc-ingestion-comparison.json`, `mpc-unapplied-corrections-diagnosis.json`, and the full `pr1000-modern/estimation__estimation_with_mpc/` run artifacts.

## Completed MPC retrieval plots

[estimation/retrieving_mpc_observation_data.py](examples/tudatpy/estimation/retrieving_mpc_observation_data.py): correct wrapped right ascension, UTC date axes, per-object legends and label spacing; clarify optional satellite environment setup and the UTC numeric filter convention. Commit [fe295ac](https://github.com/tudat-team/tudatpy-examples/commit/fe295ac20f9b319afa396d4e0439e49dd5e67f35), pushed to PR #157. Full final run passed in 70.5 seconds on the spare CPU with all four figures visually reviewed and no deprecations. The final prose-only UTC correction was checked to leave the executable AST unchanged (`source-revalidation.json`). The optional TESS block lacked its external kernel and is not claimed validated.

## Completed observation-wrapper compatibility correction

[observations_setup/__init__.py](src/tudatpy/estimation/observations_setup/__init__.py), [regression test](tests/test_tudatpy/test_data_deprecation_compatibility.py): restore lazy parent access to `observations_setup.observations_wrapper`; legacy function lookup retains its warning and user call-site attribution. Commit [e5b0de89b](https://github.com/tudat-team/tudatpy/commit/e5b0de89b76dcd41ae5ea8e779ced7a2b80b1b10), PR #1000. All 163 deprecation-compatibility tests passed (`.validation/examples-pr157/observations-wrapper-regressions.log`). The original MEX script now passes the missing-attribute failure and proceeds with the expected IFMS warning. Full residual comparison subsequently passed with identical arrays; see the MEX entry below. The original failure is preserved in `pr1000-legacy/estimation__mex_open_loop_residuals/missing-wrapper-failure.log`.

Removal instructions for the wrapper bridge are in [the compatibility README](src/tudatpy/data/README.md), added in commit [1c5430188](https://github.com/tudat-team/tudatpy/commit/1c5430188).

## Completed MEX example correction and comparison

[estimation/mex_open_loop_residuals.py](examples/tudatpy/estimation/mex_open_loop_residuals.py): use named X-band values for IFMS ingestion. Commit [efb9162](https://github.com/tudat-team/tudatpy-examples/commit/efb916297a2373ea86967681c3357de70551e21b), pushed to PR #157. Full modern run: 124.6 seconds; original-script run on the spare CPU: 289.7 seconds. Both figures visually reviewed, all 66,118 residuals and 25,969 subset residuals exactly identical between the two implementations; RMS 19.25/7.12 mHz. The stored-reference difference is not a migration regression.

Modern warnings: none. Original warnings: 28 DeprecationWarnings (including two pre-existing invalid-escape warnings) and one deprecated-interface UserWarning; native reference-point overwrite messages are retained by the old script. Evidence: `.validation/examples-pr157/mex-legacy-modern-comparison.json` and both phases' `estimation__mex_open_loop_residuals/` artifacts, including copied CSV outputs in `runtime-files/mex_output/`.

## Completed asteroid optimization presentation correction

[pygmo/asteroid_orbit_optimization/aoo_optimization.py](examples/tudatpy/pygmo/asteroid_orbit_optimization/aoo_optimization.py): apply layout to both generation figures; separate 3D panels and leave room for axis labels/titles; correctly identify NSGA-II in the prose. Commit [a180873](https://github.com/tudat-team/tudatpy-examples/commit/a1808737e0dbc374d042fee6d0bff6ab0a36986b), pushed to PR #157. Full 25-generation / 48-member run passed in 249.3 seconds on the spare CPU, with all eight numerical arrays finite and no deprecations. All five figures reviewed. Final margin corrections reused the saved optimum and reproduced the seeded first generation, rerunning only trajectory extraction and plots; subsequent export checks reuse saved orbit arrays. Evidence: `pr1000-modern/pygmo__asteroid_orbit_optimization__aoo_optimization/plot-revalidation.json` and `orbit-plot-data.npz`.

## Figure export audit

The validation harness now explicitly includes all three 3D axis labels in tight PNG exports. A border audit found seven older images with clipped text across six examples. Four short examples were rerun with exactly identical numerical arrays, the asteroid design-space plots were replayed from saved arrays, and both complete low-thrust trajectory inspections were rerun without recomputing the already validated 200×442 grid. All seven repaired images were visually reviewed; the final border audit is clear. Evidence: `.validation/examples-pr157/figure-border-audit.json`, `export-repair.log`, `replot-low-thrust.log`, and `figure-export-repair-results.json`. The low-thrust right-margin source adjustment is published in [c5e40d6](https://github.com/tudat-team/tudatpy-examples/commit/c5e40d6a553dfd5e17bcc45250fe30cf2dff3a20).

## Implemented corrections awaiting remaining verification

| File | Correction | Remaining work |
| --- | --- | --- |
| [estimation/load_pds_files.py](examples/tudatpy/estimation/load_pds_files.py) | Preserve all cached files on a day instead of repeating the first; add archive-listing timeouts and HTTP status checks. | Focused duplicate-file check passed; heavy callers pending. |

## Exclusions and open validation issues

- `estimation/improved_estimation_with_mpc.py` is excluded at the user's request and flagged for a separate correction. Its satellite option is ignored, space observations are dropped, and the weight plot is skipped. It has not been accepted or corrected here.
- Space-Track live validation needs locally configured credentials and has not run.
- The initial WISE-preview compatibility failure is corrected by the legacy-filter commit above. The original ingestion prefix now passes without modifying the example; this does not replace its pending full execution.
- GRAIL/MRO full runs, original-example compatibility, and PR #905 validation are still in progress or pending; see the progress log.

## Completed GRAIL SPICE-fit validation

`estimation/grail_spice_fit.py` completed all 20 full-day fits in 3,680 seconds with seven workers. All five figures (20 panels) visually match reference trends; all 20 saved 2,881 × 7 arrays are finite. No Python or worker-log deprecations/errors. No source correction was necessary. Evidence: `.validation/examples-pr157/grail-spice-final-audit.json` and `pr1000-modern/estimation__grail_spice_fit/`. Other heavy GRAIL/MRO runs remain in progress or queued.

## CI cleanup and publication boundary (2026-09-12)

Seven superseded PR #1000 Build and Test w/ Conda runs were cancelled and confirmed `completed/cancelled`: 34716665861, 34716478528, 34716383039, 34716205540, 34715859915, 34715531385, 34715386848. Latest build [34717639571](https://github.com/tudat-team/tudatpy/actions/runs/34717639571) was retained; latest pre-commit run succeeded. Examples branch had no Actions runs. Audit: `.validation/ci-cleanup/cancellation-requests.json`. No commits or pushes until all corrections to each branch are complete.

## GRAIL ODF propagation failure under correction

The initial full seven-arc script returned exit code zero, but April 8 propagation stopped early twice with `SPICE(NOFRAMECONNECT)` just after midnight. This run is failed validation. `estimation/grail_examples_functions.py` and `estimation/grail_odf_estimation.py` now request attitude coverage on adjacent days while preserving the single-day observation selection, and reject failed propagation/inversion results before saving fitted products. A full seven-arc rerun is queued after the current MRO work. Validation harness diagnostics now detect fatal native messages in main and worker logs; the original failure is preserved under `pr1000-modern/estimation__grail_odf_estimation/`. These corrections remain local and uncommitted.

## Additional local validation evidence

- GRAIL attitude correction: `grail-attitude-coverage-check.json` reproduces the old April 9 frame-connection failure and verifies 1,561 sampled epochs over each buffered day (plus the failing epoch), for all seven arcs. All non-attitude input lists are identical. Full estimation rerun remains queued.
- GRAIL residual analysis: all four complete April cases passed, with 11,481–11,482 residuals per case and both figures/eight panels reviewed. No deprecations or native failures. Evidence: `grail-residuals-final-audit.json`.
- Original MPC estimation: full unchanged script passed in 43.1 seconds; six figures reviewed, expected Tudat warnings recorded. The default new and old station positions differ by up to 7.1 km, explaining the fitted-state difference. A diagnostic full run of the modern model with legacy station coordinates agrees within 5.9e-13 rad in residual history and 0.055 m in fitted position components. Evidence: `mpc-station-comparison.json`, `mpc-full-legacy-modern-comparison.json`, and `mpc-matched-station-diagnostic/comparison.json`. The diagnostic copy is not a change to either example branch.

## Fresh parent import compatibility correction (local, uncommitted)

Files: [estimation initializer](src/tudatpy/estimation/__init__.py), [regression tests](tests/test_tudatpy/test_data_deprecation_compatibility.py), [removal guide](src/tudatpy/data/README.md). A fresh public parent import previously returned the raw kernel `observations_setup` module, bypassing the Python compatibility bridge and breaking three original covariance/estimation examples. Explicitly select the Python package when initializing `tudatpy.estimation`. All 165 compatibility tests pass, including both public import forms in separate fresh interpreters with alias identity and warning call-site checks. All three full original-script reruns subsequently passed and their seven figures were reviewed; failed runs are preserved in `pr1000-legacy/`. Evidence: `fresh-parent-import-regressions.log`.

## JUICE integration-step plot labels (local, uncommitted)

[propagation/juice_flybys.py](examples/tudatpy/propagation/juice_flybys.py): label the horizontal axis as time since J2000, not cumulative computation time; include seconds on the integration-step axis. A full quick rerun remains pending. The original synchronous-rotation model differs from the modern PR #157 default IAU model, so adaptive grids and sampled states are not expected to be identical; the seven closest-approach altitudes agree exactly and both figures were reviewed. The synchronous model has an unimplemented rotation derivative with a zero fallback, an existing kernel limitation; this is not changed in the current compatibility correction.

## Bulk observation filtering performance (local, uncommitted)

[SingleObservationSet](include/tudat/simulation/estimation_setup/singleObservationSet.h) removed selected rows by repeated vector erases and a full time-bound scan after every deletion, making large TNF time filters quadratic. An instrumented first-arc setup reached `original_observations.filter_observations` and stalled there; the six active MRO workers were stopped (all seven configured arcs remain required). Bulk removal now validates indices before mutation, compacts all row-aligned vectors in one pass, and updates bounds once. [Native tests](tests/test_tudat/src/astro/orbit_determination/unitTestObservationsProcessing.cpp) cover sparse/prefix/suffix/all/empty removal, observation/weight/residual/dependent-variable alignment, invalid indices, and a 20,000-observation time filter. Kernel and native-test rebuild completed: all six native cases and 216 Python compatibility tests pass. Full corrected GRAIL reruns also passed; MRO runs remain in progress. Evidence: `mro-setup-probe.log`, `mro-filtering-stop.json`, and `bulk-filter-kernel-build.log`. The main batch manager is paused until rebuild verification succeeds; the serial compatibility batch continues.

Bulk-filter verification update: the new kernel (`d3ef8ab9d316e0afdccc1ba3a4259ec4914603e5be3345c051f77d049d7d9983`) passes all 216 Python compatibility tests. The 4,000-row benchmark is 164 times faster; 100,000 rows filter in 0.040 s with the requested epochs retained. Actual first-arc MRO filtering takes 0.239 s and setup reaches the initial-propagation stage in 89.6 s. Native test compilation is still finishing; the main mission queue remains paused. Evidence: `bulk-filter-benchmark-comparison.json`, `mro-setup-probe-after.json`, and `post-build-python-compatibility.log`.

The original complete coupled-dynamics example passed in 2247.9 s; all 13 arrays and all 14 figures exactly match the validated modern run. The original covariance/estimation retries now run with the fresh-parent import correction. No new commits or pushes have been made since the publication-boundary clarification.

The three original covariance/estimation scripts now pass in full (18.7, 41.8 and 45.5 s), with all seven figures reviewed and expected public-call-site compatibility warnings. Covariance differences are small relative to their scale; the dynamical-model arrays match exactly. Modern JUICE completes all seven flybys in 32.3 s without warnings; both figures, including the corrected epoch/step units, are reviewed. Evidence: `spare-followup.log` and per-example `modern-comparison.json`.

## Legacy MPC plot time units (local, uncommitted)

[src/tudatpy/data/mpc/_legacy.py](src/tudatpy/data/mpc/_legacy.py): the original retrieval example revealed that `plot_observations_temporal` labels TDB seconds since J2000 as years. Correct the label without changing data or the deprecated interface. A focused plot replay verified unchanged coordinates and the corrected rendered label (`legacy-mpc-time-label.json/png`). The original example also labels its directly plotted UTC seconds simply “Date”; that source remains unchanged for compatibility testing (the modern example already uses calendar dates).

Bulk-filter kernel and native regression verification completed: all six observation-processing tests pass (1,672 assertions). One new test initially expected publicly accessible NaN bounds for an empty set; corrected it to assert the existing public exception behavior. The mission queue is resumed for all six full-year MRO intervals, followed by the four pending modern reruns. Original MEX rerun on the rebuilt kernel passes in 80.2 s with all five arrays and both figures identical to the previous validated results. The legacy MPC time-label focused replay preserves all coordinates and its exported figure was visually checked (`legacy-mpc-time-label.json/png`).

Original MPC retrieval completed in 57.0 s with four figures reviewed. All 3,566 shared coordinates match the modern reader exactly; the seven extra original rows are satellite records (MPC code 275 / Note 2 S), excluded by the modern reader as documented. Original asteroid optimization completed all 25 generations and 48 individuals in 108.0 s; all eight numerical arrays match exactly. Its original overlapping 3D layouts are recorded as an existing presentation limitation; corrected modern layouts were already validated. Evidence: `mpc-retrieval-row-comparison.json`, `pr1000-legacy/pygmo__asteroid_orbit_optimization__aoo_optimization/modern-comparison.json`.

## Original-example execution configuration

The original hodographic optimization has the same missing multiprocessing main guard already fixed in the modern branch. To prevent recursive worker startup, the separate original-example worktree adds only a main guard around island creation and subsequent statements; an AST comparison verifies that all original computational statements, settings and Tudat API calls remain identical. This configured-baseline run remains pending and will be labelled explicitly. Patch: `.validation/examples-pr157/legacy-hodographic-main-guard.patch`; provenance: `legacy-local-configuration.json`. The original MEX local-path configuration remains recorded separately.

Scheduling update: the final GRAIL ODF, GRAIL residual and MEX reruns now run in a separate bounded slot (at most three pool workers) alongside annual MRO (two workers) and one reviewed serial original example. The final six-worker MRO estimation scheduler waits for both mission managers to finish. No scientific settings or arc counts changed. Logs: `grail-mex-final-reruns.log`, `remaining-modern.log`, `legacy-serial-final.log`, `final-modern-scheduler.log`.

The GRAIL ODF plot source also labels two hour-valued horizontal axes as days. Corrected both labels to hours in `estimation/grail_odf_estimation.py`. The currently running estimation uses the earlier compiled source, so its seven plot exports will be replayed from the saved numerical products after it finishes; no repeat dynamics integration is needed for this presentation correction.

GRAIL ODF model interpretation: the first three corrected arcs complete and reduce RMS residuals, but retain large/negative radiation-pressure scale factors also present in the earlier run. This is an existing limitation of the two-iteration unconstrained fit, not caused by the API migration or the attitude-coverage correction. Corrected the introductory research-quality claim, stated the actual degree/order 256 used in propagation (500 coefficients loaded), identified GRAIL rather than MRO inputs, and explained the scale-factor limitation. Scientific settings remain unchanged; full seven-arc validation and plot review are still pending.

## MRO transponder-delay migration (local, uncommitted)

[Annual MRO example](examples/tudatpy/estimation/mro_tnf_residuals_analysis.py) still called deprecated `ObservationCollection.set_transponder_delay`. Preserve TNF transmitter/receiver station delays while setting the spacecraft delay directly in each TrackingData ancillary vector before conversion. A vehicle-system default alone would be overridden by these explicit vectors. Updated the [real-data short-arc regression](tests/test_tudatpy/test_data_input_tracking_residuals.py) to check retained delay vectors, 59 compressed samples, millihertz residuals, and absence of native deprecation messages. One completed old-call annual interval (index 1) and partial interval 0 outputs are preserved for numerical comparison; remaining workers were stopped and verified gone before the corrected full rerun. The main manager is paused until the corrected annual run finishes.

The first focused test attempt exposed misleading [TrackingData getter documentation](src/tudatpy/data_input/tracking_data/expose_tracking_data.cpp): these three getters accept no key argument and return dictionaries. Corrected those docstrings and the new call sites. A two-job kernel rebuild is running for the docstrings, followed by the focused test and full six-interval rerun. Evidence: `mro-transponder-stop.json`, `mro-transponder-short-arc-test.log`, `ancillary-doc-kernel-build.log`. No additional commits or pushes.

Baseline-count correction after file audit: only MRO interval 1 has the full saved residual products; interval 0 had reached compression but had not saved its residual products when stopped. Earlier progress text claiming two completed intervals was premature. The docstring-only kernel rebuild succeeded; the focused transponder-delay regression is running again.

The corrected real-TNF short-arc regression passes in 23.59 s, including full delay-vector preservation and no native/Python deprecation messages. The documentation-only rebuild produced kernel SHA `188873891ebc01b850e4dcaf644346350c4a505584fe196068fa15293222f413`. All six corrected annual intervals are now rerunning with two workers. The preserved interval-1 baseline has 1,055 range samples (1.6751 m RMS) and 43,195 Doppler samples (6.2634 mHz RMS). The harness now explicitly rejects native deprecation diagnostics in modern runs; existing completed modern logs were rescanned (`modern-native-deprecation-audit.json`).

Original Galilean-moon estimation passed in 2273.3 s with all seven arrays exactly matching modern results; both figures reviewed. The original mission downloader completed its mission operations but then failed on a stray final `plt.show()` without a pyplot import or any plots. Its configured-baseline retry removes only this final statement; the modern example already has that correction. Original failure logs and source/configuration hashes are preserved (`legacy-local-configuration.json`).

GRAIL ODF full validation complete: all seven arcs finish in 1671.4 s without native failures or deprecations. Postfit RMS is 0.917–2.731 mHz; all output arrays are finite/aligned and propagated histories cover every observation interval. All seven figures (28 panels) match reference trends and were visually reviewed after hour-label replay, with all saved numerical file hashes unchanged. The existing unconstrained-force-model limitation is explicitly documented. Evidence: `grail-odf-final-audit.json`, `grail-odf-replot.log`, and per-example `source-revalidation.json`.

The original Earth–Mars transfer-window script likewise reached its final `plt.show()` without importing pyplot. Its configured baseline adds only the missing import, already present in the modern example; its full-grid retry is queued. All original computational statements are AST-identical. Provenance: `legacy-local-configuration.json`; prior logs retained beside its result.

## MRO estimation plot completeness and failure handling (local)

[mro_tnf_estimation.py](examples/tudatpy/estimation/mro_tnf_estimation.py) no longer discards arcs 5 and 6 from the RSW comparison figure: all seven fitted arcs will be shown. Like the corrected GRAIL fit, it now checks propagation/inversion success before saving fit products. Full modern and original estimation runs are queued; this correction is not yet validated or published.

Scheduling now uses four GRAIL workers, two annual MRO workers and one serial worker. After GRAIL finishes, the four-worker MRO estimation slot may overlap the two-worker original annual pass. All numerical libraries remain limited to one thread; at most eight active compute threads including local review. Modern GRAIL ODF/residual and MEX final reruns are complete and reviewed; the original Cassini, MGA, asteroid design-space and Himmelblau runs are also reviewed.

## Split angular-observation weights (local)

[singleObservationSet.h](include/tudat/simulation/estimation_setup/singleObservationSet.h) used an observation index as the offset into flattened component weights when splitting a set. A four-row, two-component reproducer shows the second part receiving rows 1–2 weights instead of rows 2–3 (`split-weights-before.json`). Multiply the starting offset by the observable dimension; scalar Doppler splitting is unchanged. A concise [native regression](tests/test_tudat/src/astro/orbit_determination/unitTestObservationsProcessing.cpp) checks all component weights, times and observations across three parts for both count and gap splitters. Kernel/native rebuild runs with one compiler while seven example workers continue; verification pending; this bug predates the PR but was found while tracing the active compression path.

Annual MRO transponder migration comparison: all 21 saved numerical files for completed March–April interval 1 are byte-identical to the preserved deprecated-setter baseline (`mro-transponder-interval1-comparison.json`). The interval still emits four **separate** native date-helper deprecations. Changed `time_representation.add_days_to_datetime(date, 1.0)` to `date.add_days(1.0)` in [mro_tnf_residuals_analysis.py](examples/tudatpy/estimation/mro_tnf_residuals_analysis.py). The current full-year numerical run is retained; its raw logs/results will honestly retain those warnings. An exhaustive replay of the four configured filter dates and an AST comparison will verify the only changed path and identical cutoffs. Do not classify the currently executing source as warning-free. Source before this final change: `.validation/examples-pr157/mro-before-date-helper-fix.py`.

Original low-thrust validation completed: full200×442 grid and both trajectory inspections (2203.8 s), all five figures reviewed; all88,400 delta-v values and both epoch arrays exactly match modern data. Baseline plot limitations include an empty colour scale, incorrect distance labels and an extra mass division in the plotted acceleration; these are already corrected in the modern branch. The configured original mission-downloader retry passed (64.4 s). The configured original Earth–Mars retry passed (42.7 s): full200×500×2 delta-v pickle and all five figures are byte-identical to modern output. Per-example `modern-byte-comparison.json` records evidence.

Retained CI result: [run34717639571](https://github.com/tudat-team/tudatpy/actions/runs/34717639571) completed successfully on Windows, macOS and Linux for published PR #1000 commit `428185127`. This does not validate the later local corrections. Seven superseded runs remain cancelled; no further commits, pushes or CI triggers have been made during this continuation. Audit: `.validation/ci-cleanup/retained-latest-status.json`.

Queue refinement to avoid idle waiting: after explicit acceptance of modern annual MRO and completion of the rebuild checks and original serial queue, four MRO estimation workers may overlap the remaining four GRAIL workers. Original annual MRO waits for the GRAIL slot to finish, then uses two workers alongside estimation. No input intervals, arc counts or scientific settings change; all active computation remains capped at eight. `modern-annual-acceptance.json` must only be written after the complete numerical/plot audit and warning-free equivalent date-path replay; raw execution warnings remain preserved.

Angular split-weight verification is complete: rebuilt kernel `d722dfed…` passes all seven native observation-processing cases (1,716 assertions), the focused Python reproducer and 202 selected Python compatibility/boundary/real-TNF checks. The earlier 216-test run covered a different selection; the latest actual count is 202, not the previously anticipated 217.

Modern annual MRO verification is complete with explicit source replay: all six intervals, 5,520 range and 221,078 Doppler samples, six figures visually reviewed. Range RMS is 1.675–3.975 m per interval; Doppler RMS is 4.506–9.951 mHz. The raw full-run result retains all 24 deprecated date-helper diagnostics. The final expression is AST-equivalent and all four configured date cutoffs are identical without warnings; `modern-annual-acceptance.json` and per-example `source-revalidation.json` record this combined evidence without overwriting raw warnings. The seven-arc modern MRO estimation run is now released, with four workers alongside four original GRAIL workers.

GRAIL original/modern comparison: all seven original ODF arcs pass the numerical audit and their figures are reviewed. A SPICE-only diagnostic using both the original environment interpolation grid and original observations reproduces the original residuals exactly; replacing ramps alone does not remove the difference. The 637 compressed observed values are exactly equal; precise epochs differ by at most 2.4e-14 s while float epochs are identical. Replacing only the modern `Time` objects with the legacy objects makes the residuals exactly equal, and the ancillary-setting comparison is empty. The remaining difference (at most about 4.4e-5 Hz) therefore comes entirely from the sub-double epoch representation, not ramps or observation metadata. Evidence: `grail-matched-times-diagnostic/comparison.json`, `grail-matched-times-diagnostic/ancillary-differences.json`, and `grail-observation-input-diagnostic/input-comparison.json`.

The original 20-day GRAIL SPICE fit completed in 6258.2 s. Its saved array and all five figures are byte-identical to the modern run; the figures had already been reviewed panel by panel. The original annual MRO residual analysis also completed: 5,520 range and 220,312 Doppler samples across all six intervals, with expected compatibility warnings and no native failures. All six figures were compared visually with the modern products and show the same physical trends; the small filtering/count differences are recorded in `mro-annual-legacy-audit.json`. The original three-mission MRO range analysis passed and all five of its differently arranged baseline figures were reviewed against the eight modern figures.

The final modern MRO estimation source expresses propagation/environment limits in TDB, updates the spacecraft entry in every TNF link-delay vector before observation conversion, closes each result pickle through a context manager, plots all seven arcs, and rejects failed propagation or inversion products. The previous complete seven-arc run is archived as comparison evidence. The original pre-refactor example reaches estimator construction for all seven arcs but its nominal dynamics propagation terminates before the first residual update. A first-arc comparison confirms all 32 compressed sets have identical ordering, sample counts, link definitions, reference link ends, observed values and complete ancillary settings; epochs agree within 60 ns. This rules out the deprecated compression and transponder-delay bridges. Parameter ordering and two alternative propagation-bound choices have also been ruled out. Evidence: `mro-legacy-modern-input-comparison.json`.

The corrected final modern rerun was deliberately interrupted after 1,063 s because a concurrently launched setup probe caused the validation harness to relocate the shared source-level `mro_outputs` directory. That made artifact provenance ambiguous even though no source or numerical error had appeared. All related processes were stopped and verified absent; the modern and original source hashes were restored to `75a88423...` and `6ac950da...`. The interrupted result remains preserved in `pr1000-modern/estimation__mro_tnf_estimation/result.json`. Setup diagnostics and the clean full rerun will execute sequentially so each output has unambiguous provenance.

The sequential setup comparison exposed that the attempted TDB correction had coupled two distinct intervals: it used the broad nominal calendar arc for both environment interpolation and dynamics propagation. For arc 0 this starts roughly 21 hours before the first retained observation, whereas the original propagates from one hour before the first observation. The modern source now keeps the nominal converted TDB bounds for environment coverage, then derives its propagation bounds from the converted observation collection. The modern and original propagation and observation bounds now match exactly; parameter and empirical-arc counts also match, and the broader environment grid changes the initial position by at most 34 micrometres. Evidence: `.validation/examples-pr157/mro-setup-boundary-comparison.json`.

A focused first-arc modern estimator-constructor propagation with these final bounds completed in 716.7 s without native errors. Applying only the broader environment interval to the original legacy path did not cure its early state propagation failure: it still terminated 1,320 s after the start. This control rules out environment coverage as the sole explanation for the original-example failure and keeps the compatibility-path investigation open. Evidence: `pr1000-modern-diagnostics/mro_constructor_exact_bounds/result.json` and `pr1000-legacy-diagnostics/mro_constructor_broad_environment/result.json`.

The same first-arc legacy-path termination was then reproduced with the pre-PR-1000 baseline kernel built from `213fd175a0abbc1a4b9c2c61c35baf5636797e03`. It reported `State with NaN` and stopped propagation at exactly `378732114.683926`, while still completing the estimator constructor, matching the updated-kernel legacy control. The isolated baseline needed the reviewed legacy `Trk234Processor` adapter because that baseline directly aliases the deprecated name to the raw tracking-data processor, plus the baseline namespace for Doppler compression; neither adjustment changes the dynamics setup. This establishes that the termination predates the current PR 1000 kernel changes, so it is not a compatibility regression to correct in this PR. Evidence: `.validation/examples-pr157/mro-baseline-constructor-comparison.json` and `.validation/examples-pr157/pr1000-baseline-diagnostics-mro-constructor-adapter-v3.log`.

The final clean modern seven-arc MRO TNF rerun completed in 14,183.8 s. It emitted no deprecation diagnostics or native failures. The final audit confirms finite inputs, parameters and state histories in every arc, with post-fit residual RMS lower than pre-fit RMS throughout. Both generated figures were reviewed. The arc-5 post-fit along-track state RMS is 46.682 m, consistent with the prior complete run's 46.709 m and therefore expected for that arc. Evidence: `final-modern-mro-estimation-audit.json` and `estimation__mro_tnf_estimation-review-1-1.png`.
